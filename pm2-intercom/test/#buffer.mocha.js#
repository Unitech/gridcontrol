
process.env.DEBUG='network,api';

var fs      = require('fs');
var network = require('../index.js');
var should  = require('should');
var path    = require('path');
var request = require('request');

describe('Bufferization', function() {
  var n1;

  
  it('should start current node', function(done) {
    n1 = new network({
      peer_api_port : 10000
    }, done);  
  });

  
});
